{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "statusMessage": "Formateando con Prettier + ESLint...",
            "timeout": 60,
            "command": "jq -r '.tool_response.filePath // .tool_input.file_path // empty' | { read -r f; [ -n \"$f\" ] || exit 0; npx --no-install prettier --write --ignore-unknown \"$f\"; case \"$f\" in *.ts|*.tsx|*.js|*.jsx|*.mjs) npx --no-install eslint --fix \"$f\";; esac; } 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
